{
  "name": "provenance",
  "version": "0.1.2",
  "private": true,
  "description": "An open specification and reference implementation for recording the sources that shape software.",
  "scripts": {
    "build": "turbo run build",
    "check": "turbo run check",
    "dev": "turbo run dev",
    "generate": "turbo run generate:examples",
    "generate:check": "turbo run generate:check",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck"
  },
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@11.27.0",
  "devDependencies": {
    "turbo": "^2.10.13"
  }
}
